// photo_face.h — declares the pre-converted image data consumed by
// DispDriverGc9a01RoundClock::setPhoto(). See photo_face_data.cpp (generated
// by scratchpad/convert_photo.py from the source PNG, not hand-edited) for
// the actual pixel data.

#pragma once

#include "lvgl.h"

extern const lv_image_dsc_t photo_face_man;
